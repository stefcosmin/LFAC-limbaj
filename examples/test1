int a;
int b;

int sum(int x, int y)
{
 return x+y;
}

int a = 1;
int b;

void sum(int x, int y)
{
 Print(x-y);
}

class point{
    int x;
    int y;
    int z;

    float distance_from_origin(){
        int new_x;
        int new_y;
        
        return x * x + y * y;
    }
};

main()
{
    floppa = 0;
    int m = 1; 
    int n = 2;
    int n = 2;

    point3d p3;
    point2d p2;
    point legit;
    legit.z=23;
    legit.x=24;
    legit.dot_product();
    legit.do_a_backflip(3,4);
    int dist = legit.distance_from_orgin();

    int point_sum;
    int s=m+n*n;
    int point_sum = legit.z +legit.x;

    Print(a-b);
    Print(sum(a-b));

    int o;
    int p;
}
